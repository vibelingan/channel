# Catalog Price Repair - Execution (2026-09-21)

Status: recovery fixes passed full local validation and independent review; integration, release and live repair pending.
Branch: `feat/catalog-price-repair-handoff`

**Current phase:** `validate`.

**Current/next MIU:** recovery hardening pending full validation; next review/deploy, live audit/apply, reconciliation and acceptance.

## Baseline and Authority

This record updates the execution status in [HANDOFF.md](HANDOFF.md) and
[INVESTIGATION-2026-09-17.md](INVESTIGATION-2026-09-17.md). Their historical evidence
remains historical; neither old counts nor old green checks establish today's result.

| Baseline at this snapshot | Value |
| --- | --- |
| Target HEAD | `88a6690` |
| Remote main | `origin/main` at `52ebca7` |
| Remote test | `origin/test` at `fd6c1b4` |
| Original remote repair branch | Three existing commits, ending at `88a6690` |
| Worktree preparation | Safely fast-forwarded from `4e403fc` to `88a6690`; existing work preserved |

The main/test values above are remote-tracking baselines, not the divergent local
main/test refs or proof of a deployed SHA. Recheck remote heads before delivery.
The working diff contains recovery changes in the server repair implementation,
its regression tests, the repair CLI and its tests. These changes are not in HEAD.
No merge, deployment or cloud data mutation has occurred in this continuation.

The user authorized complete, documented repair and deployment with honest blockers
and independent work continuing. This documentation assignment writes only this
file in the repair worktree: no code, unrelated root, commit, push or cloud changes.

## Preserved Scope

- Inventory the **whole catalog**, all families and publication states, including
  drafts, archived, unlinked and manual records. Page in bounded batches until
  `nextId: null`; screenshots, public counts and historical totals are not limits.
- Repair eligible missing/invalid summaries only from each product's own validated
  completed source evidence and existing offer/pin policy. Preserve manual authority,
  including zero and explicit empty manual mode. Do not substitute same-name prices,
  flatten quantity tiers or automatically reprice existing different numeric prices.
- The write allowlist is `alibabaCatalogPricing` and `alibabaPrimaryOfferKey`, plus
  internal `alibabaLinkRevision` and `updatedAt`. Preserve publication, manual prices
  and controls, media, classification, source ownership/review metadata, approved
  detail snapshots and approval receipts. Conflicts and invalid evidence stop writes;
  legitimate quote-only and review-required records remain explicitly accounted for.
- Summary repair changes list/card pricing, **not approved SKU detail pricing**.
  The SKU normalizer correction needs a separate bounded replay of retained,
  hash/length-verified raw evidence into private offers/detail candidates, followed
  by a fresh summary audit. Approved details retain their old content until explicit
  review/approval. No fresh supplier sync, auto-reapproval or publication is hidden
  inside either operation; unsupported sale units/invalid tiers stay deferred.

## Evidence and Validation

Current authentication and test results below are supplied by the implementation
owner for this snapshot; the doc writer inspected the handoff, investigation and
working diff, but did not rerun those checks or access cloud data.

| Check / evidence | Current result and boundary |
| --- | --- |
| CloudBase management authentication | `READY`, bound to `diversity-123-d9grnqfux221323bb`; this is not app-admin authentication |
| App-admin access | `CHANNEL_ADMIN_TOKEN` absent; no local admin password |
| GitHub environment | Secret name `E2E_ADMIN_PASSWORD` exists; its name does not supply a local password or prove login works |
| Recovery regression checks | Server suite: 50 passed; CLI suite: 6 passed; both failure findings reproduced before fixes |
| Existing focused checks | Scoped suite: 68 passed |
| Static/contract checks | Full workspace typecheck, E2E typecheck, Biome and CloudBase SDK contract verification passed |
| Full tests and builds | All nine commands passed: workspace types, E2E types, Biome, SDK contract, root tests, site build, function builds, packaging, isolated function-artifact smoke. Logs: ignored `output/catalog-price-repair/run-20260921-014535-36885/`. Integration and same-SHA CI still pending. |
| Independent recovery review | No blockers; 6 CLI and 50 server tests passed again, plus 18 in-memory crash/persistence scenarios. No live or power-loss durability claim. |
| Live evidence | Private historical evidence is absent from this worktree; no current executable inventory/apply manifest or live acceptance result |

The investigation's **1,081 products / 1,064 eligible diagnostic omissions** are
reported September 17 counts, not today's totals or an executable manifest. Its
older browser/build/deployed-SHA observations do not validate this working diff.
Do not put tokens, passwords, raw products or private manifests into tracked docs,
public CI logs or artifacts.

## Recovery Work Implemented

### Server: retain results when a later row is uncertain

**What / why:** a guard, mutation or readback exception after an earlier successful
write could discard the page's confirmed results; lease-release failure could mask
them too. The server now retains confirmed rows, marks the uncertain row `error`,
stops later writes with `write-unconfirmed`, and preserves results while appending
`lease-release-unconfirmed` when release cannot be confirmed. Private error text is
not returned.

**Tests written / validation:** injected failures before/after commit, during
readback/lease renewal, combined write/release failure, and thrown/false release;
assert retained outcomes, no later writes and no private error leakage. The server
regression suite's 50 passing tests are reported above. **Result:** implemented,
not deployed. **Engineering rationale:** a page is not an atomic all-or-nothing
write; preserve confirmed evidence without counting a possibly committed row as
verified or attempting an unsafe rollback.

### CLI: retain attempts before validation and prohibit replay

**What / why:** the CLI previously validated a response and public readback before
saving the response, losing partial evidence on failure. It now persists a pending
attempt before the request and the received response before validation. Failures
retain the checkpoint, mark the attempt `unconfirmed` and manifest
`reconciliation-required`, and reject reuse of that manifest.

**Tests written / validation:** transport loss, partial acknowledgement and failed
public readback prove durable attempt/response storage, no checkpoint advance and
no replay. CLI suite: 6 passed. **Result:** implemented, not live-exercised.
**Engineering rationale:** a failed request can still have committed writes; a new
audit alone could classify them as already valid and omit their preservation checks.
Earlier coverage did not prove retained multi-row results and durable failed-attempt
evidence together; these regressions exercise those failure paths explicitly.

## Required Recovery Procedure

1. On partial/ambiguous apply, stop. Retain the original private manifest, page plan,
   hashes, `page.attempt`, any response and last confirmed `nextApplyIndex` with
   restricted local permissions (0600). A lost response is not proof of no write.
   Do not edit status/hashes, advance the cursor or rerun the old manifest.
2. Reconcile **every originally planned eligible product**, not only acknowledged
   `repaired` rows. Include the uncertain row that may have committed and later
   untouched rows, across attempted pages and the unattempted remainder. Read each
   current full product through authorized access; match its own product/link/source
   identity, its own planned proposed price (currency, amount and tiers), and its
   retained `protectedHash` using the repair's canonical hashing rules. Only the
   allowlisted internal revision/timestamp differences are exempt from preservation.
3. Record each row privately as confirmed applied, unchanged/not applied, or
   conflicting/unresolved. An unchanged price is not a successful repair; an equal
   price is insufficient if protected fields differ. Investigate mismatches and
   concurrent edits without overwriting them. Missing reads remain unresolved.
4. For published records, verify the public API against the current effective price;
   for confirmed repairs it must match that product's proposed price. Include
   uncertain-but-committed rows even when the server never marked them `repaired`.
   Verify protected approved detail/receipt state separately. Preserve private
   readback/preview evidence for drafts. Do not treat a fresh `valid-source` label
   as a substitute for these checks.
5. Resolve every uncertainty and failed public/preservation check, and confirm the
   primary sync lease (exclusive permission to write source-linked products) is
   idle/released through its normal fenced ownership rules. Do not force-release a
   lease, change scheduler configuration, bypass a hash or auto-rollback products.
6. **Only after reconciliation**, create a new full audit under a new manifest name
   against current records and the verified API origin. Never replay the old
   manifest. Review the new eligible/deferred outcomes before bounded apply, retain
   both evidence sets, then repeat private/public verification and the full audit.

## Remaining Phases and Truth Conditions

| Phase | Work / condition required before it can be called complete |
| --- | --- |
| Finish local validation | Collect final exit codes/logs from the running full tests and builds; all required checks pass on the final diff. Verify site and function bundles, packaging and artifact smoke checks for the affected deployment contexts. Re-run affected checks after any fix. |
| Review and integration | Independently review the recovery changes and full original three-commit repair scope, including SKU normalization/manual-save behavior. Record the reviewed release SHA and same-SHA required CI success; recheck remote main/test and deployment activity, preserving concurrent work. Never merge test into main. |
| Deployment | Use the existing reviewed test/release workflow. Completion requires successful workflow evidence and actual site/function health reporting the intended SHA at the target URLs, with public smoke checks and idle sync constraints confirmed. A local build or management login is insufficient. |
| Live audit | Obtain app-admin access; run a fresh authenticated, read-only whole-catalog audit to terminal cursor, binding origin, full current records/revisions, proposed prices and protected hashes. Account for every record and explicit deferred reason; historical projected copies cannot authorize apply. |
| Bounded apply | Apply only eligible unchanged plans with exact page hashes and fenced writes. Retain durable per-page attempts and verify every repaired product privately and every published repair publicly before advancing. Any ambiguity invokes the recovery procedure above. |
| Acceptance and closure | Fresh full inventory accounts for initial plus new/changed records, with zero unexplained eligible omissions. All protected fields remain preserved. Real desktop/mobile cards and details cover all families and supported price modes, manual controls, distinct same-name records and draft previews; retain private evidence and list unresolved/deferred outcomes honestly. |
| Separate SKU replay/detail work | Classify current raw evidence, verify retained payload hashes/lengths, and review a bounded replay plan without supplier sync. Verify corrected private offers/candidates and run a new summary audit. Public SKU-detail correction is not complete until explicitly reviewed/approved details and real API/UI checks agree. |

After deployment and credential readiness, the existing CLI accepts `audit`,
`resume-audit` and `apply`: `node scripts/catalog-price-repair.mjs <mode>
<private-manifest-path> <verified-api-origin>`. Supply `CHANNEL_ADMIN_TOKEN` only
through the process environment from a current authorized operator session.
`resume-audit` resumes read-only scanning, not an uncertain apply. Keep evidence in
ignored restricted local output. The final `.after.json` inventory and exit code
2 for remaining eligible rows are not optional closure details.

## Blockers and Next Actions

- **Integrated validation pending:** the current repair working tree passed all nine
  local commands. Integrate current main and validate the combined tree before release;
  do not substitute the pre-integration checks for same-SHA CI.
- **App-admin credential missing:** obtain a current authorized session securely.
  An authorized operator can supply credentials locally, or approved CI can consume
  its existing environment secret without exposing it. Do not try to retrieve a
  secret value from its GitHub name, reset an account or bypass application auth.
  Live audit/apply and authenticated acceptance remain blocked until access works.
- **Current private evidence missing:** after deployed-SHA and auth verification,
  rebuild the complete live audit and preservation baseline. Separately recover and
  verify retained raw evidence before SKU replay; do not fabricate today's totals.
- **Not released:** the recovery diff still needs completed validation, independent
  review and same-SHA CI/deploy evidence. No live repair or approved-detail correction
  is claimed. Continue independent local checks, build/review preparation and
  anonymous public checks while credential-dependent work is blocked; do not wait
  for unrelated subcategory/pagination work.

## Deviations

- Two reproduced recovery bugs were fixed before release, extending the original
  repair work: server failure could hide confirmed rows, and CLI failure could lose
  attempt evidence. The conservative choice retains results and stops for explicit
  reconciliation. Blind retry, silent cursor advancement, automatic rollback or
  simply replacing the manifest would conceal potentially committed writes and
  were not chosen. Both fixes have regression coverage; full local validation passed.
  Integrated CI/deploy verification remains pending. No catalog scope, approval
  authority or protected-field policy changed.